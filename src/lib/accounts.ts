// Legacy stub — account helpers are now in src/lib/portfolio.ts
// Kept to avoid breaking any imports during transition
export { getPortfolioAccountIds as getUserAccountIds } from "@/lib/portfolio";
